[project]
name = "little-aiclient"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "openai>=3.13.0",
    "prompt-toolkit>=3.0.53",
    "rich>=15.0.0",
]

[project.scripts]
chat = "client:main"
little-ai = "client:main"

